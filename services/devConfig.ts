
// --- CÔNG TẮC CHẾ ĐỘ DEV ---
// Chuyển thành `false` để sử dụng API thật và dữ liệu từ localStorage.
// Chuyển thành `true` để sử dụng dữ liệu demo và không gọi API.
export const DEV_MODE_ENABLED = true;
